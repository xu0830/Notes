### SQL Server



```sql
DBCC IND('数据库名', 表名， -1) 

DBCC TRACEON(3604)
DBCC PAGE('数据库名', IAMPageId, DatapageId, 1[查看全部信息]) 

```

IAM Page 索引页 

Data Page 数据页

8KB



### 三大范式

第一范式，确保表中每列的原子性，也即是不可拆分

第二范式，确保表中每列与主键有关，一个表中只能保存一种数据，不可以保存多种数据

第三范式，确保每列与主键列直接相关，而不是间接相关，月薪、年薪（冗余）

### Pivot

```sql
SELECT SalesAgent, India, US, UK
FROM tblProductSales
PIVOT
(
	SUM(SalesAmount)
	FOR SalesCountry
	IN ([India], [US], [UK])
)
AS PivotTable
```

### Exists 、 NOT EXISTS、 NOT IN、 IN

### 读写分离

数据库是系统性能短板

二八原则

80%的请求聚焦在20%的数据上

80%的请求都是查询，20%是增删改

##### 一主多从，发布订阅

写库:  增删改，数据最准确

从库：查询，数据订阅发布服务器，不限制从库个数；

发布服务器：主库增删改操作后，推送日志到发布服务器；从库订阅，基于日志完整数据同步；



![image-20220221121558092](C:\Users\xucanjie\AppData\Roaming\Typora\typora-user-images\image-20220221121558092.png)

##### Sql Server 配置

1、配置发布服务器（注意服务器名称统一）

2、新建发布（注意共享路径）

3、新建订阅（可以订阅多个）

```sql
use master go 
select @@servername;
select serverproperty('servername')
--	检查名称
sp_dropserver 'old_server_name'
sp_addserver 'current_computer_name', 'local'
```

快照复制：变化频率低，变化数据大

事务复制：同步效率高，变化频繁，每个步骤都包含

合并复制：通常用于服务器到客户端的环境中，会冲突  

### 分库分表

### 分库

#### 垂直分库

按业务拆分

#### 水平分库

按数据拆分，多个库数据结构一样，地区、时间

### 分表

#### 垂直分表

数据表中字段数据过长，拆分常用字段、不常用字段

#### 水平分表

按时间等划分数据

### 分库分表后数据查找问题

跨库跨表查询

1、设计时注意尽量避免跨库跨表（租房网按地区、电商按时间）

2、不允许link 

3、实时性要求不高，总库，晚上同步

4、要求实时，提供服务获取，避免直连数据库

5、分页查询，需总数 (NoSql统计数据 |  无总数，仅下一页 | 其他技术，搜索引擎）

6、业务妥协 



### 约束

建表时加约束

建表后加约束

alter table [表名] add constraint [约束名称]  [约束类型]   {  ([foreign key reference 表名（主键列名）])  | unique (列名)}

#### 主键约束 

primary key

#### 外键约束 

foreign key reference 表名（主键列名）

##### 级联删除

ALTER TABLE 表名

add contraint 约束名

foreign key（列名） reference 表名 （列名） on delete cascade

#### unique约束

unique

#### check约束

check (值限制条件表达式, number<10000  ） 

#### default约束

default



### like通配符

%  0个或多个

_    匹配单个字符

[]   范围匹配，括号中字符中的一个

[^]  不在括号中字符内的单个字符



### 字符串函数

CHARINDEX(子字符串， 目标字符串) 子串位置

PATINDEX('%子字符串%'， 目标字符串) 子串位置

LOWER()  字母小写转换

UPPER() 字母大写转换

LEN() 字符长度

LTRIM() 去右边空格

RTRIM() 去左边空格

LEFT(目标字符串， 从左边开始到第N位的字符串)  LEFT('abcdf'， 3)  => 'abc'

RIGHT(目标字符串，从右边开始到第N位的字符串)  RIGHT('abcd'， 3) => 'bcd'

SUBSTRING(目标字符串，左边起始位置，子串长度)  SUBSTRING('abcdefg',  3, 4)  =>  'cdef'

REPLICATE(目标字符串, 重复次数)    REPLICATE('abc'， 3)  =>  ‘abcabcabc’

REVERSE(目标字符串)  字符串翻转

REPLACE(母字符串，子字符串，替换后的字符串) 字符串替换 REPLACE('abcde'， ‘cd’， ‘ss’)  => 'absse'

STUFF(目标字符串，起始位置，指定长度， 替换后的子串) 替换指定长度的子字符串  STUFF('abcdef', 2, 3, 'tt') =>  'attef'

### 索引

优化数据的查询、处理速度

目录，快速定位要查找的速度，不必扫描整个表

缺点：占用存储空间，并不是越多越好

#### 聚集索引

逻辑顺序与物理存储顺序是一致的，最多只能有一个，可以没有

#### 非聚集索引

可以有多个 
选择 小数据类型的列 

**CREATE** **CLUSTERED** |**NONCLUSTERED** (不指定，默认非聚集索引) 

**INDEX** 索引名 

**ON** 表名（列名）

 **WITH** ( **DROP_EXISTING** = **ON** ) ]   参数设置（删除已存在， 不存在需设置OFF）

ignore_dup_key = on | off   是否忽略重复键值插入

##### 复合索引

多个列建立的索引 

ON 表名  (**列名， 列名**)

#### 填充因子

pad_index=on 设置填充因子生效

读写比 

读小于写：  50--70

读写各一半： 80-90

With （ pad_index=on, fillfactor=50 ）填充因子  指定创建索引时，每个索引页的数据占索引页大小的百分比，值越小，分页越多

### 视图

虚拟表，一个或多个表的查询，保存查询定义，实际不包括数据

与表的区别：表存储数据，视图存储查询语句（s索引视图除外）

简化查询，增加数据的保密性

仅简化查询，并不提高查询速度，增加了维护成本  

#### 标准视图

CREATE VIEW 视图名

AS

​		SELECT 字段 from 表名

GO   

**ALTER** VIEW 视图名

AS

​		SELECT 字段 from 表名 

GO

#### 索引视图

被具体化，创建了索引，显著提高了查询性能，聚合了很多行的查询，不适合频繁更新

数据是实际存在的，对视图数据的操作会影响原表  

CREATE VIEW 视图名

WITH SCHEMABINDING

AS

​		 SELECT (不允许使用*)

GO

#### 分区视图

UNION ALL

### 存储过程

SQL语句集合，编译后存储在数据库中，可以加速SQL语句的执行

系统存储过程，master数据库，其他数据库可直接调用

execute / exec 存储过程名 参数（多个参数，逗号隔开）

提高程序的通用性和可移植性，多次调用，

更有效的管理数据库权限

提高SQL执行速度，减轻服务器的负担 

缺点

提高维护成本，占用数据库空间

赋值

SET 一次只能赋值一个字段

SELECT  可同时赋值多个字段

CREATE PROCEDURE 存储过程名

@参数名称 数据类型，@参数名称 数据类型 (输入、输出参数 )

AS 

BEGIN

​		sql 语句 

END 

GO

### 事务

#### ACID

##### 原子性

一个事务中的操作要么全部成功，要么全部失败

##### 一致性

数据库总是从一个一致性的状态转换到另外一个一致性的状态

##### 隔离性

事务的修改在最终提交前，对其他事务是不可见的

##### 持久性

#### 隔离级别

##### read uncommit

读未提交，可能会读到其他事务未提交的数据，脏读

##### read commit 

读已提交，两次读取结果不一致，叫做不可重复读

##### repeatable read

可重复读，mysql默认级别，每次读取结果都一样，但有可能幻读

##### serializable 

串行，给每一行读取的数据枷锁，一般不适用，会导致大量超时和锁竞争问题

事务一旦提交，所做修改就会永久保存在数据库中

#### ACID靠什么保证

原子性由 undo log 日志保证，记录了需要回滚的日志信息，事务回滚是撤销已经执行成功的sql

一致性由其它三大特性保证、程序代码要保证业务上的一致性

隔离性由 MVVCC 保证

持久性 由内存 + redo log 保证，mysql 修改数据同时在内存和 redo log 记录这次操作，

宕机时可以从 redo log 恢复

```
InnoDB redo log 写盘，InnoDB 事务进入 prepare 状态
如果前面 prepare 成功， binlog 写盘，再继续将事务日志持久化到 binlog，
如果持久化成功，那么 InnoDB 事务则进入 commit 状态
（在 redo log 里面 写一个 commit 记录）
redo log 的刷盘会在系统空闲时进行
```

#### MVCC

##### 多版本并发控制

读取数据是通过一种类型快照的方式将数据保存下来，这样读锁就和写锁不冲突了，

不同的事务 session 会看到自己特定版本的数据，版本链

MVCC 只在 READ COMMITTED 和 REPEATABLE READ 两个隔离级别下工作

其他两个隔离级别和 MVCC 不兼容，因为 READ UNCOMITTED 总是读取最新的数据行，

而不是符合当前事务版本的数据行

SERIALIZABLE 则会对所有读取的行都加锁

聚簇索引记录中有两个必要的隐藏列

**trx_id**：用来存储每次对某条聚簇索引记录进行修改的时候的事务 id

**roll_pointer**：每次对哪条聚簇索引记录有修改的时候，都会把老版本写入 undo 日志中

这个 roll_pointer 就是存了一个指针，指向这条聚簇索引记录的上一个版本的位置，

通过它来获得上一个版本的记录信息（注意插入操作的 undo 日志没有这个属性，因为它没有老版本）



**已提交读和可重复读的区别就在于它们生成 ReadView 的策略不同** 

开始事务时创建 readview，readview维护当前活动的事务 id，即未提交的事务 id，排序生成一个数组

访问数据，获取数据中的事务 id（获取的是事务 id 最大的记录），对比 readview：

如果在 readview 的左边（比 readview 都小），可以访问（在左边意味着该事务已经提交）

如果在 readview 的右边（比 readview 都大）或在 readview 中，不可以访问，获取 roll_pointer，取上一版本重新对比（在右边意味着，该事务在 readview 生成之后出现， 在 readview 中意味着该事务还未提交）

已提交读隔离级别下的事务在每次查询的开始都会生成一个独立的 readview，而可重复读隔离级别则在第一次读的时候生成一个 readview，之后的读都复用之前的 readview

这就是 MySQL 的 MVCC，通过版本链，实现多版本，可并发读-写，写-读。通过 readview 生成策略的不同实现不同的隔离级别

begin  tran

begin  try

​	commit tran

end  try

begin  catch 

​	rollback  tran

end  catch

### mysql 主从同步原理

mysql主从同步的过程:

mysql 的主从复制中主要有三个线程： master（binlog dump thread）、slave（I/O thread、SQL thread）

Master 一条线程和 Slave 中的两条线程

- 主节点 binlog， 主从复制的基础是主库记录数据库的所有变更记录到 binlog。

  binlog 是数据库服务器启动的那一刻起，保存所有修改数据库结构或内容的一个文件

- 主节点 log dump 线程，当 binlog 有变动时， log dump 线程读取其内容并发送给从节点
- 从节点 I/O 线程接收 binlog 内容，并将其写入 relay log 文件中
- 从节点的 SQL 线程读取 relay log 文件内容对数据更新进行重放，最终保证主从数据库的一致性

主从节点使用 binlog 文件 + position 偏移量 来定位 主从同步的位置，从节点会保存其已接收到的偏移量，

如果从节点发生宕机重启，则会自动从 position 的位置发起同步

由于 mysql 默认的复制方式是异步的，主库把日志发送给从库后不关心从库是否已经处理，

可能发生主库宕机，从库处理失败，从库升为主库后，日志就丢失了。

由此产生两个概念

#### 主同步复制

主库写入 binlog 后强制同步日志到从库，所有从库都执行完成后才返回给客户端，

但性能会受到严重影响

#### 半同步复制

从库写入日志成功后返回 ACK 确认给主库，主库收到至少一个从库的确认就认为写操作完成

### MyISAM 、InnoDB区别

#### MyISAM

不支持事务，但每次查询都是原子的，

支持表级锁，每次操作都是对整个表加锁

存储表的总行数

一个 MyISAM 有三个文件： 索引文件、表结构文件、数据文件

（每次查询都需要两次操作，查询索引文件 -> 查询数据文件）

采用非聚集索引，索引文件的数据域存储指向数据文件的指针。

辅助索引与主索引基本一致，但是辅助索引不用保证唯一性

#### InnoDB

支持ACID的事务，支持事务的四种隔离级别

支持行级锁级外键约束，因此可以支持写并发

不存储总行数

一个 InnoDB 引擎存储在一个文件空间（共享表空间，表大小不受操作系统控制，一个表可能分布在多个文件里），也有可能为多个（设置为独立表空，表大小受操作系统文件大小限制，一般为 2 G），受操作系统文件大小的限制

主键索引采用聚集索引（索引页存储在数据文件中），辅助索引的数据存储主键的值，

因此从辅索引查找数据，需要先通过辅助索引找到主键值，再访问辅助索引，最好使用自增主键，

防止插入数据时，为维持B+树结构对文件的大调整。

### mysql 索引类型及对数据库性能的影响

普通索引： 允许被索引的数据列包含重复的值

唯一索引： 可以保证数据记录的唯一性

主键： 一种特殊的唯一索引，在一张表中只能定义一个主键索引，主键用于唯一标识一条记录，

使用关键字 PRIMARY KEY 来创建

联合索引： 可以覆盖多个数据列

全文索引： 通过建立 倒排索引，极大的提升检索效率，解决判断字段是否包含的问题，

是目前搜索引擎使用的一种关键技术。可以通过 ALTER TABLE table_name ADD FULLTEXT(column)

使用ElasticSearch 更好



索引可以极大的提高数据的查询速度

通过使用索引，可以在查询的过程中，使用优化隐藏器，提高系统的性能

但是会降低插入、删除、更新表的数据，因为在执行这些操作时，还要操作索引文件

索引需要占用物理空间，除了数据表占数据空间外，每个索引还要占一定的物理空间，

如果要建立聚簇索引，那么需要的空间就会更大，

如果非聚簇索引很多，一旦聚簇索引改变，那么所有非聚集索引都会跟着变
