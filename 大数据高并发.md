### 分布式

多台服务器完成一台服务器做的事

分布式有代价

分布式第一定律就是不要用分布式

分布式锁

数据一致性，分布式事务

稳定性

### 缓存

缩短网络路径，加快响应速度

缓存穿透

​		利用不存在的缓存数据进行访问，

​				保存null

​				过滤查询（布尔过滤器）

缓存击穿

​		热点数据过期，大量请求涌入数据库

​				数据不过期，只更新

缓存雪崩

​		大量key同时失效

​				随机过期时间

​                缓存数据均衡分配

![image-20220223090955642](C:\Users\xucan\AppData\Roaming\Typora\typora-user-images\image-20220223090955642.png)

#### 浏览器缓存

Cache-Control

##### Etag

MD5摘要（不管文件大小，摘要长度固定），缓存过期，http header 加 if-None-Match，请求带摘要，对比摘要，200|304

![image-20220222222613366](C:\Users\xucan\AppData\Roaming\Typora\typora-user-images\image-20220222222613366.png)

#### CDN缓存

用户提交域名 ——>  浏览器对域名进行解释  ——>  CDN域名服务器返回指定域名的CName记录  ——>  对CName记录进行再解释

  ——>  得到CDN缓存服务器的地址  ——>  根据IP地址发出访问请求  ——>  缓存服务器通过内部DNS得到真实主机地址

  ——>  向真实主机发出请求，并把请求返回客户端  ——>  客户端浏览器得到请求数据后解析 

#### 反向代理缓存

##### Nginx缓存

#### 服务器缓存

本地缓存+分布式缓存

#####  MemoryCache

 注册服务、依赖注入

Miscrosoft.Extens  ions.Caching.Abstractions

当前进程内存的缓存

空间问题

集群数据共享问题（分布式缓存）

多服务实例 数据共享问题（Redis）

![image-20220223072338449](C:\Users\xucan\AppData\Roaming\Typora\typora-user-images\image-20220223072338449.png)

##### Redis

Miscrosoft.Extensions.Caching.Redis

<img src="C:\Users\xucan\AppData\Roaming\Typora\typora-user-images\image-20220223085733064.png" alt="image-20220223085733064" style="zoom:50%;" />

#### ResourceFilter

实例化控制器后，拦截Action，不进入Action方法

#### 中间件缓存

![image-20220223090449082](C:\Users\xucan\AppData\Roaming\Typora\typora-user-images\image-20220223090449082.png)

services.AddResponseCaching();

app.UseResponseCaching();

配合ResponseCacheAttribute，配置缓存条件

### 搜索引擎

#### 全文搜索

关系型数据库无法完成人性化模糊搜索

#### Lucene

#### Solr

#### ES

全文检索，分词建索引，检索时分词搜索  

最大程度上搜索出相关结果

### 秒杀系统

多线程  高并发

1、数据库悲观锁，无法满足高并发（局限）

2、乐观锁--version--多个并发只有一个成功（局限）

限流，限制请求到达数据库

防超卖

高性能

Redis （单线程模型，无线程安全问题）

incr、decr

### 刷榜

直播礼物实时统计排行

Redis

数据类型 zSet，key - values + score

缓存预热

### gRPC

Remote Procedure Call

.Net Reomting: .Net 平台独有的，不支持跨平台

gRPC：高性能、开源和通用的RPC框架，面向服务端和移动端，基于HTTP/2设计。

### 分布式异步队列

FIFO，先进先出

异步化处理，时间换空间

削峰，高可用

高扩展性

重试机制

缺陷

没有同步的体验

### 分布式锁

多进程（分布式）

分布式锁：跨进程的互斥机制来控制共享资源的访问

#### 1、基于数据库实现

数据库中创建一个表

表中包含详细描述方法的字段

创建唯一索引

执行方法，先插入数据，成功插入数据后获取锁

执行完成后删除对应的行数据并释放锁

问题：

数据库宕机，热备-主从

失效机制，有限器，定时删除过期锁

重入机制，自身线程重入

阻塞机制，调用方重试，等待锁失效而非失败。

#### 2、Redis

setnx(key, value)获取锁

expire命令设置锁超时时间

delete(key)释放锁

问题

setnx、expire 执行命令有间隔时间，期间服务挂掉，

改用Set(key, value, time) ( 会覆盖原始key值) +setnx

执行时间超过锁过期时间

另启线程，延长锁过期时间

锁key为guid（锁自动释放，其他线程获取锁后，原线程执行完后又释放锁）

#### 3、基于Zookeeper/Consul

 key-value

