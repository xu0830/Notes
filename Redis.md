# Redis

## 简介

用 C 语言开发，性能的键值对数据库，

支持多个数据库，每个数据库的数据是隔离的不能共享，并且基于单机才有，集群没有数据库的概念，

默认支持16个数据库（可通过配置databases支持更多，无上限）

每个数据库对外都是一个从 0 开始的递增数字命名，客户端与 Redis 建立连接后会自动选择 0 号数据库，可使用 SELECT 命令更换数据库

不支持自定义数据库名称，以编号命名

数据存在于内存，读写速度快

可用于缓存、分布式锁、消息队列

提供多种数据类型以支持不同的业务场景

支持事务、持久化、<font color='red'>Lua脚本、多种集群方案</font>

不支持为每个数据库设置不同的访问密码

多个数据库之间并不是完全隔离的

FLUSHALL 命令可以清空一个 Redis 实例中所有数据库中的数据

这些数据库更像是一种命名空间，而不适宜存储不同应用程序的数据

比如可以使用0号数据库存储某个应用生产环境中的数据，使用1号数据库存储测试环境中的数据，但不适宜使用0号数据库存储A应用的数据而使用1号数据库B应用的数据，不同的应用应该使用不同的Redis实例存储数据

非常轻量级，一个空Redis实例占用的内存只有1M左右

## 与 Memcached 对比

### 共同点

基于内存的数据库，常用于缓存

都有过期策略

高性能

### 区别

| Redis                                                        | Memcached                          |
| ------------------------------------------------------------ | ---------------------------------- |
| 支持多种数据类型                                             | 只支持string                       |
| 支持数据持久化                                               | 不支持                             |
| 有灾难恢复机制（加载持久化数据）                             | 无                                 |
| 内存不足时，可将不用的数据放到磁盘                           | 报异常                             |
| 原生支持集群模式                                             | 无原生的集群模式，需要客户端实现   |
| 使用单线程、IO多路复用的网络模型（Redis 6.0引入了多线程IO）  | 使用多线程、非阻塞IO复用的网络模式 |
| 支持发布订阅模型、Lua脚本、事务等                            | 不支持                             |
| 过期数据的删除策略使用了惰性删除和定期删除                   | 惰性删除                           |
| 底层模型不同，客户端间通信的应用协议不一样，Redis直接构建了VM机制 |                                    |
| value值大小不同，Redis最大可达到512M                         | 1M                                 |

## 优势

高性能

数据存在于内存，读写速度快

高并发

单机情况下，MySQL这类数据库QPS (Query Per Second每秒请求数) 在1w左右（4核8G），Redsi可达到10w+，甚至30w+

## 数据类型

### string

简单动态字符串（SDS）

**常用命令**： set、get、strlen、decr、incr、setnx、setex等

**应用场景**： 需要计数的场景，用户访问量、文章转发量等

### list 

双向链表

**常用命令**： lpush、rpop、rpush、lpop、lrange、llen等

**应用场景**： 发布订阅（消息队列）

### hash 

数组+链表

**常用命令**：hset、hget、hexists、hkeys、hvals等

**应用场景**：存储对象数据

### set 

无序、去重、可实现交集、并集、差集的操作

**常用命令**： sadd、spop、smembers、sismember、scard、sunion等

**应用场景**：需要存放的数据不能重复、需要获取多个数据源的交集、并集、差集

### sorted set 

zset） 与set相比，增加了一个权重参数score，能够按score进行排序，有序、去重

**常用命令**：zadd、zcard、zscore、zrange、zrevrange等

**应用场景**：需要根据某个权重进行排序的场景，直播系统中的礼物排行榜、消息弹幕等

## 单线程模型

![image-20210514090922635](C:\Users\XWC\AppData\Roaming\Typora\typora-user-images\image-20210514090922635.png)

### 文件事件处理器

Redis基于Reactor模式开发自己的网路时间处理器，称为文件事件处理器

由于文件事件处理器是单线程，所以一般称Redis为单线程模型

Redis通过 IO 多路复用来监听多个客户端连接（监听多个socket）

包含四个部分

多个套接字（socket）：客户端连接

IO多路复用程序：支持多个客户端连接的关键

文件事件分派器（dispatcher）：将多个socket关联到相应的事件处理器

事件处理器：连接应答处理器、命令请求处理器、命令回复处理器

### 单线程速度快的原因

数据存在于内存，读写速度快

单线程操作，避免了死锁、线程上下文切换代理的性能开销

采用非阻塞 IO 多路复用机制

### Redis6.0 前采用单线程的原因

单线程编程容易，易于维护

Redis 性能瓶颈不在CPU，主要在内存和网络

多线程存在死锁、线程上下文切换等问题，甚至影响性能

### Redis 6.0 引入多线程原因

为了提高网络 IO 读写性能

Redis 只在网络数据读写这类耗时操作上使用了多线程，执行命令仍然是单线程顺序执行，因此不需要担心线程安全问题

## 过期数据机制

### 过期数据判断

通过过期字典（hash表）保存数据过期时间

过期字典的键指向 Redis 中的某个键，过期字典的值保存了数据的过期时间（毫秒精度的时间戳）

### 过期数据删除策略

采用定期删除+惰性删除

**定时删除**： 设置过期时间时，同时创建一个定时器，定时器会在过期时间到来时自动删除过期数据，节约内存，但CPU压力大

**定期删除**： 每隔一段时间抽取一批数据过期检查。CPU、内存压力不是很大

**惰性删除**： 在取出数据时进行过期检查，节约CPU性能、内存压力大

## 内存淘汰机制

解决问题: 过期删除策略会漏掉某些过期数据，可能导致大量过期数据堆积在内存，造成内存溢出。

### volatile-lru(least-recently used) 

从已设置过期时间的数据中，淘汰最近最少使用的数据

### volatile-ttl

从已设置过期时间的数据中，淘汰将要过期的数据

### volatile-random

从已设置过期时间的数据中，随机淘汰数据

### allkeys-lru

从所有数据中，淘汰最近最少使用的数据

### allkeys-random

从所有数据中，随机淘汰数据

### noeviction

禁止淘汰数据，内存不足时直接报错

### volatile-lfu（least frequently-used）

从已设置过期时间的数据中，淘汰最不经常使用的数据

### allkeys-lfu

从所有数据中，淘汰最不经常使用的数据

## 持久化机制

### 实现

单独创建 fork( ) 一个子进程，将当前父进程的数据库数据复制到子进程的内存中，然后由紫进程写入到临时文件中，持久化的过程结束了再用这个临时文件替换上

次的快照文件，然后子进程退出，释放内存

当两种方式同时开启时，数据恢复Redis会优先选择 **AOF** 恢复

### RDB

默认持久化方式，按照一定的时间周期策略把内存中的数据以快照的形式保存在磁盘的二进制文件（snapshot快照存储），对应产生的数据文件为dump.rdb

通过配置文件中的 save 参数来定义快照的周期

#### 特点

文件小，恢复速度快，对性能影响小，但实时性差

#### 配置

save 900 1		900 秒内，如果超过 1 个 key 被修改，则发起快照保存

save 300 10		300 秒内，如果超过 10 个 key 被修改，则发起快照保存

save 60  10000	60 秒内，如果 1 万个 key 被修改，则发起快照保存

### AOF

将每一个收到的写命令通过Write函数追加到文件最后

类似于MYSQL的binlog，当Redis重启时会通过执行文件中保存的写命令来在内存中重建整个数据库的内容

使用 AOF 持久化，每个命令以Redis协议的格式来保存

Redis可以在后台对 AOF 文件进行重写，使 AOF 文件的体积不会超出保存数据集状态所需的实际大小

实际上，AOF持久化并不会立即将命令写入到硬盘文件中，而是写入到磁盘缓存

所以在一定程度一定条件下，还是会有数据丢失，不过可以大大减少数据损失

通过命令 appendfsync 配置从硬盘缓存写入到硬盘文件的时间

AOF重写（bgrewriteaof命令）：将内存中的数据以命令的方式保存到新的AOF文件中，然后用新的AOF文件替换旧的AOF文件

#### 特点

文件大，恢复速度慢，对性能影响大，但实时性高

#### 配置

appendonly no/yes	    是否开启 AOF 持久化

appendfsync always		每次操作都会立即写入 aof 文件中

appendfsync everysec	每秒持久化一次(默认配置)

appendfsync no			不主动进行同步操作，默认 30s 一次

没有生成 appendonly.aof，执行以下命令

redis-cli config set appendonly yes

redis-cli config set save ""

## 事务

### 相关命令

MULTI：开启事务

EXEC:：执行事务中的所有命令

DISCARD：取消事务，放弃执行事务中的所有命令

WATCH：监听一个或多个key，如果该事务在执行前，监听的key被其他命令修改，则中断事务，不会执行事务中的任何命令

UNWATCH：取消WATCH对所有key的监听

使用MULTI命令后可输入多个命令，Redis不会立即执行这些命令，而是会放在队列中，等调用Exec命令后在原子化执行这些命令

Redis不支持回滚，因此不满足原子性

当Redis运行在AOF持久化模式下，并且appendfsync选项设为always时，具有持久性

## 原子性

命令的原子性是指，一个操作的不可再分，操作要么执行，要么不执行

多个命令在并发中不一定是原子性，可将 get 和 set 改成单命令操作 incr，使用 Redis 事务，或者使用 Redis+Lua==的方式实现

## 分布式锁

### 加锁

采用队列模式将并发访问变成串行访问

多客户端对 Redis 的访问并不存在竞争关系

Redis 中可以使用 SETNX 命令实现分布式锁，当且仅当 key 不存在， 将 key 的值设为 value

若给定的 key 存在， SETNX 不做任何动作

### 解锁

使用 del key 命令

### 解决死锁

通过 Redis 中 expire( ) 给锁设定最大持有时间，超过则自动释放锁

使用 SETNX key “当前系统时间 + 锁持有时间” 和 getset key “当前系统时间 + 锁持有的时间” 组合的命令实现   

## 集群

### 主从模式

从库（master）负责读写，从库（slave）负责读，不具备高可用

master宕机，不影响slave，只是不再提供写服务，不会在slave中选一个master

### 哨兵（Sentinel）模式

通过sentinel进程监视master、slave的运行状态

建立在主从模式基础，

为避免sentinel挂掉，一般会做sentinel集群，不和master、slave部署在同一台机器

sentinel每秒向master、slave及其他sentinel实例发送PING命令，以监视其运行状态

master宕机，sentinel会在slave中选一个master，并修改所有实例的配置

master重启后，将作为slave运行

### 集群模式

Redis提供的分布式数据库方案，解决单机Redis容量上限的问题通过分片进行数据共享

由多个节点组成，每个节点包含一主一从（或一主多从），从库仅做备用

客户端可以连接任意节点的主库进行读写

集群的整个数据库被分为16384个槽（slot）当存储一个 key-value 时，会被分配在某个槽上

## 缓存穿透

大量请求不存在缓存中的key，导致请求落在数据库上

数据在数据库没有，自然在缓存中也没有，导致用户查询时，在缓存中找不到，每次都要去数据库再查询一遍，然后返回空

（相当于进行了两次无用的查询）

#### 解决方法

拦截非法key

一般采用<font color='red'>bitmap</font>、<font color='red'>布隆过滤器</font>（Bloom Filter）

##### bitmap

将所有可能存在的数据哈希到一个足够大的 bitmap 中

不存在的数据会被这个 bitmap 拦截，从而避免对底层存储系统的查询压力

**缺点**

对于每个元素只能记录1bit信息，只能靠牺牲更多的空间、时间来完成额外功能

##### 布隆过滤器（推荐）

引入 k(k>1)k(k>1) 个相互独立的哈希函数，保证在给定的空间、误判率下，完成元素判重的过程

核心思想就是利用多个不同的 hash 函数来解决冲突（两个 URL 用同一个hash得到的值有可能相同）

为了减少冲突，可以引入多个 hash

如果通过其中的一个hash值得出某元素不在集合中，那么该元素肯定不在集合中

只有在所有的 hash 函数都存在时，才能确定该元素存在于集合中

**优点**

空间效率和查询时间都远远超过一般算法

**缺点**

存在一定的误识别率和删除困难

##### 缓存空值

即使查询结果为空，也将这个空结果缓存，但设置一个较短的过期时间

## 缓存击穿

一个key非常热点，大并发集中对这一个点进行访问，在这个 key 失效瞬间，持续的大并发就穿破缓存，直接请求数据库

### 解决方法

热点数据永不过期

在访问 key 之间，采用 SETNX (set if no exists) 来设置另一个短期 key 锁住当前 key 的访问，访问结束再删除该短期 key

## 缓存雪崩

大量缓存在同一时间失效，或者一些被大量访问的缓存（热点缓存）在某一时刻失效，导致请求落在数据库上

#### **解决办法**

设置不同的过期时间

加锁、队列、限流，避免同时处理大量请求，保证不会有大量的线程对数据库一次性读写

设置热点缓存永不失效

## 缓存预热

系统上线后，将相关的缓存数据直接加载到缓存系统

## 缓存降级

当访问量剧增、服务出现问题或非核心服务影响到核心流程的性能时

可以根据一些关键数据进行自动降级，也可以配置开关实现人工降级，保证核心服务可用







