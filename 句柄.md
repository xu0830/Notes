## 句柄

``` c#
[DllImport("User32.dll", EntryPoint = "FindWindow")]
public extern static IntPtr FindWindow(string lpClassName, string lpWindowName);
```

FindWindow	//查找一个窗口

GetWindowThreadProcessID	//获取PID或者TID

OpenProcess	//获取一个进程句柄

ReadProcessMemory	//通过窗口句柄和内存地址，来获取内存数据

WriteProcessMemory	//通过窗口句柄和内存地址来写入内存数据

